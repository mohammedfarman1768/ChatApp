import { SearchQuerySchema, SearchPaginationSchema, ReindexSchema } from '@repo/shared-validation';

export const SearchRequestSchema = SearchQuerySchema;
export { SearchPaginationSchema, ReindexSchema };
